# Revised Context Structure

```text
references/
├── topic-guide.md
├── <topic-a>.md
└── <topic-b>.md
templates/
└── <output-template>.md
examples/
├── <normal-case>.md
└── <edge-case>.md
```

Source of truth:
- <which file owns stable rules or knowledge>

Navigation:
- Use `<file>` when <condition>.
- Use `<file>` when <condition>.

Notes:
- <migration or cleanup note>
