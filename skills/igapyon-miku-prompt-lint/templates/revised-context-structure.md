# Revised Context Structure

```text
references/
├── topic-guide.md
├── <topic-a>.md
└── <topic-b>.md
templates/
└── <output-template>.md
examples/
├── <normal-case>.md
└── <edge-case>.md
```

Source of truth:
- <which file owns stable rules or knowledge>
- Version or effective date: <version/date>

Navigation:
- Use `<file>` when <condition>.
- Use `<file>` when <condition>.

Validation:
- Success criteria: <observable completion condition>.
- Evidence: <what result or artifact proves it>.

Fallback:
- If <required context is missing or conflicts>, <safe behavior>.

Notes:
- <migration or cleanup note>
